# ── Stage 1: Data Ingestion ──────────────────────────────────────────────────
requests==2.31.0        
boto3==1.34.0             
python-dotenv==1.0.0     
pandas==2.2.3             

# ── Stage 2: Transformation (install when you get there) ─────────────────────
# pyarrow==14.0.1         # writes Parquet files for Silver/Gold layers
# dbt-core==1.7.0         # SQL transformations
# dbt-athena-community==1.7.0  # dbt adapter for AWS Athena

# ── Stage 4: ML Training (install when you get there) ────────────────────────
# prophet==1.1.5           # time series forecasting
# lightgbm==4.1.0          # gradient boosting model
# scikit-learn==1.3.2      # cross-validation, metrics
# mlflow==2.9.2            # experiment tracking + model registry
# shap==0.43.0             # model explainability

# ── Stage 6: Serving (install when you get there) ────────────────────────────
# fastapi==0.104.1         # prediction API
# uvicorn==0.24.0          # ASGI server for FastAPI
# streamlit==1.28.0        # dashboard